<div id="main">
  <h1><%= "#{@user.name}  #{@title}" %></h1>
  <% if @users.any? %>
    <% @users.each do |user| %>
      <%= link_to user.name, user %>
    <% end %>
  <% end %>
</div>

<div id="sidebar">

</div>